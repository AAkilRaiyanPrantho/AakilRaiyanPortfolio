

const Headings = ( {heading} ) => {
    return (
        <div className="flex justify-center items-center">
            <h1 className="text-3xl font-semibold italic uppercase text-center outline-dashed p-4">{heading}</h1>
        </div>
    );
};

export default Headings;